{
  "name": "study-group2",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "saucedemo": "cypress run --e2e --browser chrome --config-file cypress.saucedemo.config.js",
    "workco": "cypress run --e2e --browser chrome --config-file cypress.workco.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.3.0",
    "cypress-image-diff-js": "^1.32.0",
    "cypress-image-snapshot": "^4.0.1",
    "prettier": "3.0.3"
  }
}
